---
title: "variants-list"
api: "admin"
endpoint: "/internal/v1/admin/modules/:id/variants"
method: "get"
client: "admin"
tag: "api"
---

# GET List Variants

Returns the variants of a module.

## Authentication

- Requires a valid Supabase Auth session with `admin_dashboard_role = "admin"` or `"analyst"` in the JWT.
- Uses the Supabase **admin** client (service-role key); requests bypass RLS entirely.

## Headers

| Header | Value | Required |
| --- | --- | --- |
| `Authorization` | `Bearer <token>` | Yes |
| `Accept` | `application/json` | Yes |

## Path parameters

| Name | Type | Description |
| --- | --- | --- |
| `id` | `uuid` | The parent module id. |

## Response body

`200 OK`

```json
{
  "status": 200,
  "message": "",
  "data": {
    "items": [
      {
        "id": "v3...ii",
        "module_id": "m3...hh",
        "name": "High-Autonomy Path",
        "description": "For learners with prior facilitation experience.",
        "created_at": "2026-06-29T12:15:00Z",
        "updated_at": "2026-06-29T12:15:00Z"
      }
    ]
  }
}
```

| Field | Type | Description |
| --- | --- | --- |
| `items` | `Variant[]` | Variants belonging to the module. |

### Variant object

| Field | Type | Description |
| --- | --- | --- |
| `id` | `uuid` | Variant id. |
| `module_id` | `uuid` | Parent module id. |
| `name` | `string` | Variant name. |
| `description` | `string \| null` | Variant description. |
| `created_at` | `timestamptz` | Row creation timestamp. |
| `updated_at` | `timestamptz` | Row last-modified timestamp. |

## Errors

| Status | Code | When |
| --- | --- | --- |
| `401` | `unauthenticated` | Missing or invalid JWT. |
| `403` | `forbidden` | JWT does not carry an allowed `admin_dashboard_role` (`admin` or `analyst`). |
| `404` | `module_not_found` | Module does not exist. |
| `500` | `internal_error` | Unexpected database failure. |

```json
{
  "status": 404,
  "message": "Module not found",
  "error": "module_not_found",
  "data": null
}
```
